//string interpolation : Template strings  support string interpolation, allowing ypu to embed expression directly within the stirnf, Interpolated expression are inclosed in $()


let age =29;
let message =`My age is ${age} years`;  // this is known as string interpolation
console.log(message);