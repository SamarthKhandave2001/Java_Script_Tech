Strings in java script are a fundamental data type thet represents a sequence of charachter

Strings created with single or double quotes works the same.

Search() Method: The Searc() method searches a stirng for a string(or a regular expression) and returns the position of the match.

//Returns the index number where the first match is found. Returns -1 if no match is found.

//mathc(): Returns an array of the matched values or null if no match is found.


Strings are Immutable

You cannot change a character directly.

let str = "Hello";
str[0] = "Y";  
console.log(str); // Hello (no change)


Is JavaScript String Primitive or Object?

By default, a string is a primitive data type

JavaScript automatically converts it into a String object when methods are applied

👉 This is called auto-boxing

Difference Between == and === for Strings

== → compares values only

=== → compares value + data type

Using === is always recommended.